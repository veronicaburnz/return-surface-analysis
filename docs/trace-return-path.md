# Trace Return Path

`Trace Return Path` was the working title for the method now published as **Return Surface Analysis**.

Use [`return-surface-analysis.md`](return-surface-analysis.md) as the canonical method document.

The old phrase is still useful as an action:

```text
Trace the return path to map the return surface.
```

But the public method name is:

```text
Return Surface Analysis
```
